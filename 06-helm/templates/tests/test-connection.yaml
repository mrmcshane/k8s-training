apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "05-helm.fullname" . }}-test-connection"
  labels:
{{ include "05-helm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "05-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
